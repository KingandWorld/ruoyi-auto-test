# RuoYi 接口自动化测试  -  环境变量
base:
  # RuoYi 服务地址（本地部署，端口已改为 8080）
  base_url: "http://localhost:8080"

  # 管理员登录账号
  admin:
    username: "admin"
    password: "admin123"

  # 请求超时时间
  timeout: 10

  # 默认请求头
  headers:
    Content-Type: "application/json"
    User-Agent: "RuoYi-AutoTest/1.0"